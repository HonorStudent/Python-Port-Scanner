# Python-Port-Scanner
A simple TCP port scanner written in Python using the built-in socket library. This project scans a user-specified IP address across the full TCP port range (1–65535) and reports any open ports it discovers. It demonstrates the fundamentals of network programming, socket communication, exception handling, and resource management in Python.
